# E2E Test Environment Configuration
# This file configures the FastAPI server to use E2E test runtime directories

# Server configuration
HOST=127.0.0.1
# PORT is auto-selected from available ports in 8010+ range
SESSION_TIMEOUT=3600
LOG_LEVEL=INFO

# Data directories - point to E2E runtime
DATA_ROOT=tests/e2e/runtime
DB_DIR=tests/e2e/runtime/db
CONFIG_DIR=tests/e2e/runtime/config

# Log directory - separate from production logs
LOG_DIR=tests/e2e/runtime/logs

# WebDAV - disabled for E2E tests (no WebDAV server)
WEBDAV_ENABLED=false

# Discord - disabled for E2E tests (no external API calls)
DISCORD_AUDIT_TRAIL_ENABLED=false

# Application Mode - testing mode enables mock extractor
FASTAPI_APPLICATION_MODE=testing
