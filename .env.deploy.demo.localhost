# Demo Deployment Environment File (Local)
#
# This configuration creates a local demo deployment without nginx or SSL.
# Ideal for testing deployments on localhost or in development environments.
#
# Usage:
#   node bin/deploy.js .env.deploy.demo.localhost
#
# The container will be accessible at:
#   http://localhost:8000
#
# Note: This is a demo deployment with non-persistent storage.
# All data is stored inside the container and will be lost when the container is removed.

# ============================================================================
# Deployment Configuration (DEPLOY_*)
# ============================================================================

# Required: Fully qualified domain name
DEPLOY_FQDN=localhost

# Demo deployment type (uses container-internal storage)
DEPLOY_TYPE=demo

# Local port binding
DEPLOY_PORT=8080

# Skip confirmation prompt for automated deployments
DEPLOY_YES=true

# Note: Deploy command automatically sets restart policy to 'unless-stopped'

# Use latest image
DEPLOY_TAG=latest

# Rebuild image before deploying
DEPLOY_REBUILD=true

# Uncomment to not use cached imaged
# DEPLOY_NO_CACHE=true

# Skip confirmation prompt
DEPLOY_YES=true

# ============================================================================
# Container Environment Variables
# ============================================================================

# User Account Setup
# ------------------
# If neither APP_ADMIN_PASSWORD nor APP_DEMO_PASSWORD are set, the container
# creates default accounts with passwords "admin" and "demo" respectively.
# For production deployments, ALWAYS set custom passwords:
#
# APP_ADMIN_PASSWORD=your-secure-admin-password
# APP_DEMO_PASSWORD=your-secure-demo-password
#
# These variables create/update users on container startup:
# - admin user: username=admin, roles=admin, email=admin@localhost
# - demo user: username=demo, roles=user,annotator,reviewer, email=demo@localhost
#
# For demo purposes, we'll use the defaults (admin/admin and demo/demo)

# Custom Login Message
# APP_LOGIN_MESSAGE="<h2>Welcome</h2><p>Custom login message with HTML</p>"

# Logging
LOG_LEVEL=INFO

# AI/ML Services (optional - add your keys for testing AI features)
# GEMINI_API_KEY=your-gemini-api-key-here
# GROBID_SERVER_URL=http://localhost:8070
