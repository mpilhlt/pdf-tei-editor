# Test environment for Local Sync Plugin
# This file enables the plugin for integration testing

LOG_LEVEL=DEBUG

# Test Paths - runtime data is ephemeral
DATA_ROOT=tests/api/runtime
DB_DIR=tests/api/runtime/db
LOG_DIR=tests/api/runtime/logs

# Plugin Configuration
PLUGIN_LOCAL_SYNC_ENABLED=true
PLUGIN_LOCAL_SYNC_REPO_PATH=/tmp/local-sync-test
# Disable include/exclude filters for tests
PLUGIN_LOCAL_SYNC_REPO_INCLUDE=
PLUGIN_LOCAL_SYNC_REPO_EXCLUDE=
