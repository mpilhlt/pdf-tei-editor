# Import files on startup

- Using the `IMPORT_PATHS` environment variable, you can specify a list of file directories to automatically import on application startup.
- The import script needs to be updated to automatically create the collections if they do not already exist, using the collection API.